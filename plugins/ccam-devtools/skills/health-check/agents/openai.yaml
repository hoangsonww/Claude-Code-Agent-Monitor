interface:
  display_name: "Health Check"
  short_description: "Run comprehensive health checks on the Claude Code Agent..."
  default_prompt: "Use $health-check to inspect CCAM data and complete this workflow safely."
policy:
  allow_implicit_invocation: true
