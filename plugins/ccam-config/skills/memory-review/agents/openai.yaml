interface:
  display_name: "Memory Review"
  short_description: "Review the file-based memory store via the Agent Monitor..."
  default_prompt: "Use $memory-review to inspect CCAM data and complete this workflow safely."
policy:
  allow_implicit_invocation: false
