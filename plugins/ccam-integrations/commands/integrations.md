---
description: Read-only inventory of CCAM alerts, webhook targets, and remote sources
---

Run `ccam alerts --unacked`, `ccam alert-rules list`, `ccam webhooks`, and
`ccam remote-sources`. Produce a compact inventory of open alerts, enabled
rules, webhook health, and remote sync state. Do not mutate or send anything.
