---
description: List live and persisted CCAM-launched Claude Code and Codex runs
---

Run `ccam run list` and `ccam run history --limit 30`. Summarize live handles
first, then recent persisted runs with provider, model, working directory,
status, session ID, and duration. Do not start or stop anything.
