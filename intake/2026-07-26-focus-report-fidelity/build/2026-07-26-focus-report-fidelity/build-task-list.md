# Build Task List — 2026-07-26-focus-report-fidelity

> Authored by `build-planner`, merging `technical-plan.md` (what to change) and
> `qa/test-plan.md` (what to prove) into ONE ordered, dependency-correct
> sequence. The implementer follows this top to bottom, test-first. Detailed
> enough to build without re-reading the original investigation.
>
> **Worktree (use this path for every command/file below, not the main
> checkout):**
> `/Users/sara/CODE-LOCAL/SARA/efforts/2026-07-26-focus-report-fidelity/Claude-Code-Agent-Monitor`
> Branch: `effort/2026-07-26-focus-report-fidelity`. All file paths below are
> relative to this worktree's root. No Docker stack is provisioned for this
> effort and none is needed — every done-check is `npm run test:server` /
> `npm run test:client` / a scoped `vitest`/`node --test` invocation, run
> directly against the worktree with no live service.
>
> **Important correction vs. the technical-plan's own step 1/12 wording:**
> the technical-plan was drafted assuming round-4's diff was still
> uncommitted. Per `build-brief.md`, round-4 is **already committed** on this
> branch at `2416292` (this worktree's own starting commit) — do **not**
> re-commit it or fold it into this build's final commit. Task 1 below is
> reduced to "re-verify the baseline is green," not "commit round-4 first."
> The final commit (task 17) covers only this build's own diff.

## Build order (dependency-correct, red-first)
> Tests that prove a change come BEFORE the change they guard. `[test]` =
> author a failing test; `[impl]` = product code; `[docs]` = documentation;
> `[verify]` = re-run existing suites, no new assertions. `MANDATORY
> [DERIVED-DUAL-VIEW]` = durable cure, gates done — this project has no
> `PROJECT-CONTEXT.md`/formal defect-catalog, so `DERIVED-DUAL-VIEW` is cited
> as the de facto catalog id both plans converged on ("a duration value
> computed once server-side, rendered by two independent client surfaces,
> with no shared helper and no test enforcing agreement"), per the explicit
> instruction for this build.

| # | Task | Type | Layer | File(s) | Done-check |
|---|------|------|-------|---------|------------|
| 1 | Re-verify the round-4 baseline is real, not stale — this is the ground truth everything below is built on, not a new test. | verify | server + client | (whole worktree) | `npm run test:server` → 902/902; `npm run test:client` → 403/403 across 36 files; `bash .claude/skills/file-headers/scripts/check-headers.sh` → exit 0, clean |
| 2 | Write `idleStripes.test.ts` against the not-yet-created module — red-first at import time. Include: `[]` for `undefined`/`[]` chunks; `[]` for zero-length/inverted range; one stripe per idle chunk in `{offsetPct, spanPct}` shape (assert exact key set, guards against a silent revert to `topPct`/`heightPct`); clipping a partially-overlapping chunk; dropping a fully-outside chunk; orientation-agnostic (same relative chunks at two different epochs → identical fractions); the two fixtures ported **byte-for-byte** from `FocusCalendarView.test.tsx`'s existing idle-stripe tests (its 50/50 fixture and its all-active/no-stripe fixture). Add file header. | test | client lib unit | `client/src/lib/__tests__/idleStripes.test.ts` (new) | `cd client && npx vitest run src/lib/__tests__/idleStripes.test.ts` → fails, "Cannot find module '../idleStripes'" (or equivalent) |
| 3 | **MANDATORY `[DERIVED-DUAL-VIEW]` — extract-before-reuse.** Create `client/src/lib/idleStripes.ts`: `idleStripesInRange(chunks, rangeStartMs, rangeEndMs): FractionalStripe[]`, ported verbatim from `FocusCalendarView.tsx`'s current `idleStripesForBlock` body, fields renamed `topPct`/`heightPct` → `offsetPct`/`spanPct`, first param widened to accept `FocusReportChunk[] \| undefined` (returns `[]` for undefined/empty/malformed range). File header per `.claude/rules/file-headers.md`. This must be a genuine shared module used by **both** `FocusCalendarView.tsx` and `FocusReportModal.tsx`'s `SegmentedBar` — never a second, independently re-implemented copy of the stripe math in the List view (task 9 depends on importing this, not reimplementing it). | impl | client lib | `client/src/lib/idleStripes.ts` (new) | `cd client && npx vitest run src/lib/__tests__/idleStripes.test.ts` → green, all cases including the two ported fixtures match exactly |
| 4 | Refactor `FocusCalendarView.tsx` onto the shared helper (pure move, no behavior change): delete the local `IdleStripe` interface and `idleStripesForBlock()`; import `idleStripesInRange` (and `FractionalStripe` if needed) from `../lib/idleStripes`; update the one call site and the two field renames (`stripe.topPct`→`stripe.offsetPct`, `stripe.heightPct`→`stripe.spanPct`). | impl | client component | `client/src/components/FocusCalendarView.tsx` | `cd client && npx vitest run src/components/__tests__/FocusCalendarView.test.tsx` → 13/13, **zero assertion changes**. Gate, not red-first: if any assertion needs touching, **stop** — this is a live regression against round-4, not a refactor detail (see Sequencing notes) |
| 5 | Add the registry-derived i18n completeness block: `LOCALES = ["en","ko","vi","zh"] as const`, one loop driving per-locale assertions (not 4 copy-pasted `it`s). For each locale: `report.wallClockLabel`/`report.activeLabel` must resolve to real strings (not the literal key); `report.calendar.wallClockLabel`/`...activeLabel` must **no longer** resolve (literal key string back). Plus one en-only byte-identical check against the pre-relocation strings (`"Wall clock"`, `"Agent time"`). Red-first against current locale files: new-path assertions fail (nothing there yet), old-path assertions fail in the opposite direction (still resolves today). | test | client i18n | `client/src/i18n/__tests__/i18n.test.ts` | `cd client && npx vitest run src/i18n/__tests__/i18n.test.ts` → fails on both the new-path and old-path assertions, for the reasons stated |
| 6 | **MANDATORY `[DERIVED-DUAL-VIEW]` — atomic i18n relocation.** In all 4 `client/src/i18n/locales/{en,ko,vi,zh}/plan.json` files, in the **same step**, move `report.calendar.wallClockLabel`/`report.calendar.activeLabel` up to `report.wallClockLabel`/`report.activeLabel` (same string values — key-path rename only, no translated-text change). Update `FocusCalendarView.tsx`'s two `t(...)` calls to the new path. Do not land 3 of 4 locale files and defer the 4th — a partial rename silently renders a raw i18n key string in the missed locale, and the mechanical guard (task 5) exists precisely to catch that. | impl | client i18n + component | `client/src/i18n/locales/en/plan.json`, `client/src/i18n/locales/ko/plan.json`, `client/src/i18n/locales/vi/plan.json`, `client/src/i18n/locales/zh/plan.json`, `client/src/components/FocusCalendarView.tsx` | `cd client && npx vitest run src/i18n/__tests__/i18n.test.ts` → green for all 4 locales; re-run `cd client && npx vitest run src/components/__tests__/FocusCalendarView.test.tsx` → still 13/13 |
| 7 | Update `FocusReportModal.test.tsx`'s `makeReport()` base fixture: segment 1 (`item`, 4, "Migrate auth") → `wall_ms: 30m, active_ms: 20m, idle_ms: 10m` with a 3-chunk `chunks` array (first two `active: true`, last `active: false`); segment 2 (`bug`, "npm conflict") stays `wall_ms === active_ms === 10m`, **no `chunks` field** (exercises the "no chunks → no stripe" guard). Recompute session/item/report totals to match (session: wall 40m/active 30m/idle 10m; `by_kind.item`: wall 30/active 20/idle 10; `by_kind.bug`: wall 10/active 10/idle 0). Leave `totals.wall_ms` (50m) and `totals.idle_ms` (10m) untouched. | test | client component | `client/src/components/__tests__/FocusReportModal.test.tsx` | `cd client && npx vitest run src/components/__tests__/FocusReportModal.test.tsx` → **expected red**: the existing "computes the on-item percentage" test now fails (`75%`/`25%` no longer match); everything else stays green (concurrency-ratio and idle-excluded tests are unaffected — verify this, don't assume) |
| 8 | Update the "computes the on-item percentage" assertion to the new correct values: `"75%"` → `"67%"`, `"25%"` → `"33%"` (`Math.round(20/30*100)=67`, `100-67=33`). This is the one deliberate, plan-called-out assertion edit — not a regression to chase. | test | client component | `client/src/components/__tests__/FocusReportModal.test.tsx` | `cd client && npx vitest run src/components/__tests__/FocusReportModal.test.tsx` → green again, full file |
| 9 | **MANDATORY `[DERIVED-DUAL-VIEW]` — single source for "how long."** Implement the List-view fix in `FocusReportModal.tsx`: (a) `kindTotalsAsSegments()` gains `active_ms: totals.by_kind[kind].active_ms` in its returned shape, filter changes from `s.wall_ms > 0` to `s.active_ms > 0`; (b) `SegmentedBar` gains `sizeField?: "wall_ms" \| "active_ms"` (default `"wall_ms"`), widens its segment prop to `Pick<FocusReportSegment, "kind"\|"label"\|"wall_ms"\|"active_ms"> & Partial<Pick<FocusReportSegment,"inferred"\|"inferred_reason"\|"chunks"\|"start"\|"end">>`, sizes/labels every slice by `seg[sizeField]`, adds `data-kind={seg.kind}` and `data-testid="segmented-bar-{session\|item-rollup\|project-split}"` hooks, and renders the idle-stripe overlay via `idleStripesInRange` (imported, never reimplemented) only when `sizeField === "wall_ms"` **and** the segment carries `chunks`/`start`/`end` — each stripe gets `data-testid="idle-stripe"`; (c) the per-session call site computes both `totalWallMs`/`totalActiveMs` and renders `{t("report.wallClockLabel")} {formatMs(totalWallMs)} · {t("report.activeLabel")} {formatMs(totalActiveMs)}` only when they diverge, else the plain single number; (d) the two aggregate call sites (per-item rollup, project-wide split) switch to `totalMs={item.totals.active_ms}`/`totalMs={report.totals.active_ms}` and `sizeField="active_ms"` — **the two aggregate bars must read the existing `FocusKindTotals.by_kind[kind].active_ms` field, never a new client-side rollup or computed figure invented for this.** | impl | client component | `client/src/components/FocusReportModal.tsx` | (verified by tasks 10-12's tests going green; no standalone done-check beyond re-running the file, see task 12) |
| 10 | Add: per-session header shows a labeled wall-clock/agent-time split when they diverge, plain single number when they don't. Push a second session (`"NoIdle"`, `wall_ms === active_ms === 15m`) alongside `"Worker"` (`wall_ms 40m/active_ms 30m` post-fixture). Red-first against pre-task-9 code: no `/30m 0s/` exists in "Worker"'s row yet. | test | client component | `client/src/components/__tests__/FocusReportModal.test.tsx` | Before task 9: fails (only one unlabeled number rendered). After task 9: `cd client && npx vitest run src/components/__tests__/FocusReportModal.test.tsx` → this case green |
| 11 | Add: the per-session bar overlays exactly one idle stripe (for the segment carrying `chunks`, none for the one without) — assert `container.querySelectorAll('[data-testid="idle-stripe"]')` length `1`, and that stripe's `left`/`width` (`toBeCloseTo`, not string equality) equal `(20/30)*100` and `(10/30)*100`. Add a second, dedicated "renders no idle stripe" case (single-segment, no-`chunks` fixture) asserting stripe count `0`. Red-first: no `data-testid="idle-stripe"` element exists anywhere in this file today (confirm by grep before writing the assertion). | test | client component | `client/src/components/__tests__/FocusReportModal.test.tsx` | Before task 9: fails (`0`, not `1`). After task 9: green |
| 12 | Add: per-item rollup bar and project-split bar sized by `active_ms`, matching their already-correct printed number — the direct regression test for the embedded bug. Assert both bars' per-kind slice widths are `(20/30)*100` (`item`) and `(10/30)*100` (`bug`), not the `wall_ms`-based 75/25 — use the `data-kind`/`data-testid` hooks from task 9 (fallback selector `.h-3 > div` / `.h-6 > div` only if those hooks were declined; state which approach was taken in the PR). **Required — do not drop:** inject a third kind (e.g. `detour`) into `items[0].totals.by_kind` and `report.totals.by_kind` with a large `wall_ms` (e.g. 20m) and near-zero `active_ms` (e.g. 1000ms); assert that kind's rendered slice width is near-0 (`toBeLessThan(2)` as a percentage), not the ~40% a `wall_ms`-proportional width would produce. Red-first against pre-task-9 code (with the new fixture numbers already in place from task 7): sizing still reads `wall_ms` unconditionally → produces 75/25 and a large near-zero-kind slice. | test | client component | `client/src/components/__tests__/FocusReportModal.test.tsx` | Before task 9: fails as described. After task 9: `cd client && npx vitest run src/components/__tests__/FocusReportModal.test.tsx` → green, including the near-zero case |
| 13 | **MANDATORY `[DERIVED-DUAL-VIEW]` — standing cross-view regression test.** Build one single-segment session dated "today" (`vi.useFakeTimers()`/`vi.setSystemTime`/`vi.useRealTimers()` in a `try/finally`, scoped to this one test only — every other test in this file uses literal 2026-06-10 timestamps, do not go fake-timers file-wide) with `wall_ms: 20m, active_ms: 10m, idle_ms: 10m`, two 10-minute chunks (first active, second idle). Render the modal; assert the List view's per-session header contains both `/20m 0s/` and `/10m 0s/` and exactly one `idle-stripe` at `left ≈ 50%, width ≈ 50%`. Then `fireEvent.click(screen.getByTitle("Calendar"))` (assert the focus-report fetch mock is still called only once total — no second fetch), `fireEvent.mouseEnter` the corresponding block, assert the hover popup contains the **same two numbers** and exactly one idle stripe at `top ≈ 50%, height ≈ 50%`. **Write this as a standing template, not a one-off** — add an in-file comment stating that any future field added to `FocusReportSegment` that either view renders must extend this same test, not get a separate view-local test only. Run this test against the state right after task 6 but **before** task 9 — it must fail on the very first List-view assertion (header shows only `"20m 0s"`, zero idle stripes), the literal reproduction of round-4's exact failure shape. If it does not reproduce that failure pre-task-9, the test itself is too weak — fix the test, not the sequencing. | test | client component | `client/src/components/__tests__/FocusReportModal.test.tsx` | Before task 9: fails on the List-view assertion, reproducing round-4's gap exactly. After task 9: `cd client && npx vitest run src/components/__tests__/FocusReportModal.test.tsx` → green without modification |
| 14 | Add `describe("inferredSegment / buildSessionFocusReport - inferred fallback", ...)` between the existing `"buildSessionFocusReport - activity chunks"` block (ends ~line 412) and `"buildProjectFocusReport"` (starts ~line 414). No dependency on tasks 2-13 — independent of the client work, but sequenced here (matching both plans' own placement) rather than pulled earlier; see Sequencing notes for why a single sequential implementer may still choose to do this first. 5 cases, all via the file's existing `seedSession`/`focus`/`activity`/`t`/`nextId` helpers and `stmts.upsertFocusInference.run(sessionId, cwd, kind, item_id, label, confidence, method, reason)` — do not introduce a parallel seeding helper: (1) item-kind → current `item_number`/text via `getPlanItemById`, `inferred: true`, `inferred_reason` set; (2) detour-kind → `kind: "detour"`, `item_number: null`, `label` from inference row; (3) deleted-item (`item_id` doesn't resolve) → `assert.deepEqual(report.segments, [])`; (4) unclassified verdict → `assert.deepEqual(report.segments, [])`; (5) **highest-value** — round-3-shaped idle tail: `activity()` bursts at minutes 1/4/8, `ended_at: t(130)`, one `item` inference row; assert `wall_ms === 130*60_000`, `active_ms < wall_ms`, `chunks.length === Math.ceil(wall_ms/CHUNK_MS)`, `chunks[0].active === true`, every later chunk `active === false`. **Not a red/green pair** — coverage-only, no behavior change to `inferredSegment()`; expect every case to pass on first run. If any case fails, that is a real, previously-hidden bug surfacing — stop and report it, do not adjust the assertion to match wrong behavior. | test | server | `server/__tests__/focus-report.test.js` | `node --test server/__tests__/focus-report.test.js` → new block passes immediately, all 5 cases; then `npm run test:server` → 902 baseline + 5 new = 907/907 |
| 15 | Full re-verification across both suites plus the file-header audit. Review (don't blindly regenerate) any `screens.snapshot.test.tsx` diff — none is expected (neither modal is rendered by that suite today per QA's own check); if a diff appears, treat it as a signal to investigate, not to reflexively run `-u`. | verify | server + client | (whole worktree) | `npm run test:server` → 907/907; `npm run test:client` → 403 baseline + new/updated cases (idleStripes.test.ts, i18n.test.ts additions, FocusReportModal.test.tsx's updated/added cases), `FocusCalendarView.test.tsx` still exactly 13/13 with zero assertion diff; `bash .claude/skills/file-headers/scripts/check-headers.sh` → exit 0 |
| 16 | Update docs in the same change: `ARCHITECTURE.md` (~line 1690-1701, `FocusReportModal.tsx` List description — add that the per-session bar now overlays idle chunks via the shared `idleStripesInRange` helper and the per-item/project-split bars are sized by `active_ms`); `client/README.md` (`#### FocusReportModal`, ~lines 647-654 — extend the List-view bullet to match the existing Calendar bullet's convention, ~lines 665-671); `docs/API.md` (`Get Project Focus Report`, ~lines 1204-1284 — optional clause noting both client views now consume `chunks`/`active_ms`, no wire-shape change); `server/README.md` (~lines 647-677 — same optional clause if it currently names Calendar specifically). | docs | docs | `ARCHITECTURE.md`, `client/README.md`, `docs/API.md`, `server/README.md` | Manual read-through confirming each file states the List view now carries the same idle-visibility/`active_ms`-sizing convention as Calendar; no runnable command, but re-run `bash .claude/skills/file-headers/scripts/check-headers.sh` if any doc's surrounding code fences changed |
| 17 | Commit this build's diff as one reviewed, tested commit (this build's own work only — round-4 is already committed at `2416292`, do not re-bundle it). State the visible behavior change plainly in the commit/PR summary per PO/QA: the per-session header now shows a labeled wall-clock/agent-time split whenever they diverge (previously one unlabeled, `wall_ms`-only number); the per-item and project-split bars are now sized by `active_ms`, not `wall_ms` (a kind with real elapsed time but zero active time now renders ~0 width in those two bars, previously full width, unstriped — intended fix, not a regression). Explicitly name `server/__tests__/projects.test.js`'s adjacent route-level gap (its `active_ms` assertions are neutralized by `DASHBOARD_FOCUS_IDLE_GRACE_SECONDS="0"`) as an out-of-scope follow-up in the PR description, not silently dropped. | impl | repo | (git commit) | `git status --porcelain` clean after commit; PR/commit message includes the behavior-change paragraph and the `projects.test.js` follow-up note |

## Mandatory durable-cure tasks
- **#3 — Extract `idleStripesInRange` into `client/src/lib/idleStripes.ts` before List view becomes a second consumer.** `MANDATORY [DERIVED-DUAL-VIEW]` — structural, not a patch: without this, the List-view fix (#9) would have had to re-implement Calendar's stripe math a second time, which is precisely the "no shared helper" half of the recurring pattern. Both plans require this file to exist and be imported by both consumers; it is proven behavior-preserving by the ported fixtures in #2.
- **#9 (and its guard, #12) — Aggregate bars must size off `FocusKindTotals.by_kind[kind].active_ms`, never a new client-side rollup.** `MANDATORY [DERIVED-DUAL-VIEW]` — the architect explicitly rejected inventing a client-side "rolled-up chunk count" for the aggregate bars; #12's near-zero-`active_ms` fixture case is the specific pin against a future silent revert to `wall_ms` sizing (it is the one case in the suite that fails if only the numbers-happen-to-look-right, not the sizing basis, is checked).
- **#13 — The List-vs-Calendar cross-view consistency test as a standing template.** `MANDATORY [DERIVED-DUAL-VIEW]` — this is the direct hit: it is the one test that would have caught round 4 stopping at one consumer, and per the test-plan's "Durable-cure decision," it must be written with an in-file comment marking it as the template any future `FocusReportSegment` field addition must extend, not a one-off fixture pass.
- **#6 (guarded by #5) — i18n key relocation must land atomically across all 4 locale files in the same step.** `MANDATORY [DERIVED-DUAL-VIEW]` (closest analog: a missed-locale silent fallback is this pattern's "no shared enforcement" failure mode in miniature) — a partial rename is exactly the kind of per-surface drift the registry-derived loop in #5 exists to make structurally impossible to ship unnoticed, rather than relying on a human eyeball-scan of 4 files.

No point-fix-over-structural-cure shortcut was found in the technical-plan for any of these four — both plans independently converged on the structural cure being built in this pass, so there is no open decision to surface here on that axis. (The only genuinely open, non-blocking item is whether `DERIVED-DUAL-VIEW` becomes a named `PROJECT-CONTEXT.md` catalog entry going forward — both plans and the build-brief agree this is Sara's call, not something this build decides.)

## Sequencing notes
- **Single sequential implementer — do not parallelize**, even though both source plans flag task 14 (`inferredSegment()` server coverage) as independent of tasks 2-13 and "may be done in parallel by a second contributor." There is one implementer on this build; task 14 has no file overlap with anything else in this list, so it is safe to pull earlier in the sequence (e.g., right after task 1) purely to unblock a natural checkpoint, but do not run it concurrently with client work — do the whole list top to bottom in one thread.
- **Tasks 2→3, 5→6, 7→8, and 10/11/12/13→9 are strict red-before-green pairs.** Do not implement task 9 before tasks 10-13 are written and observed failing for the stated reason — writing the test after the code defeats the point of this build's own durable-cure obligation (a test that never failed proves nothing about the fix it claims to guard).
- **Task 4 is a gate, not a red-first step.** `FocusCalendarView.test.tsx` must need zero assertion changes after the extraction. If it does, stop and treat it as a live regression against round-4's already-shipped, already-committed behavior — do not "fix" the test to make the refactor pass.
- **Task 13's pre-task-9 red run is load-bearing.** If the cross-view test does not fail on the List-view assertion when run against the post-task-6/pre-task-9 state, the test is too weak (e.g., a selector that doesn't actually reach the new markup) — fix the test itself before proceeding, do not just move on because "it'll pass later anyway."
- **No live service needed anywhere in this build.** Every done-check above is a `node --test` / `vitest` invocation or a static script (`check-headers.sh`) run directly in the worktree — no Docker stack was provisioned for this effort (per `build-brief.md`) and none of these 17 tasks requires one.
- **Stop-and-report triggers** (plan-is-wrong conditions — escalate, don't improvise a fix):
  - Task 4: any `FocusCalendarView.test.tsx` assertion needs changing after the pure refactor.
  - Task 13: the cross-view test does not reproduce round-4's failure shape pre-task-9.
  - Task 14: any of the 5 `inferredSegment()` cases fails on first run against unmodified server code (a real, previously-hidden bug, not a test-authoring error).
  - Any test outside the ones named in tasks 7-8 (concurrency-ratio, idle-excluded) breaks after the task-7 fixture change — the plan says these are unaffected; if one breaks, treat it as a signal to re-check the arithmetic, not to loosen the assertion.
  - Task 15: an unexpected `screens.snapshot.test.tsx` diff appears — investigate before running `-u`, since neither modal is expected to be rendered by that suite.
